# Generated by `mix exosphere.gen.lexicons` — DO NOT EDIT.
# Regenerate with: mix exosphere.gen.lexicons

defmodule Exosphere.Bsky.Ageassurance do
  @moduledoc """
  XRPC client functions for the `app.bsky.ageassurance` namespace.

  Generated from vendored lexicons — DO NOT EDIT.

  ## Endpoints

  - `begin` — procedure: app.bsky.ageassurance.begin
  - `get_config` — query: app.bsky.ageassurance.getConfig
  - `get_state` — query: app.bsky.ageassurance.getState
  """

  alias Exosphere.ATProto.XRPC.Client

  alias Exosphere.Bsky.Runtime

  @doc """
    Initiate Age Assurance for an account.
  """
  @spec begin(Client.t(), map()) :: {:ok, map()} | {:error, term()}
  def begin(client, body \\ %{}) do
    client
    |> Client.procedure("app.bsky.ageassurance.begin", body)
  end

  @doc """
    Returns Age Assurance configuration for use on the client.
  """
  @spec get_config(Client.t()) :: {:ok, map()} | {:error, term()}
  def get_config(client) do
    client
    |> Client.query("app.bsky.ageassurance.getConfig")
  end

  @fields_get_state %{country_code: "countryCode", region_code: "regionCode"}

  @doc """
    Returns server-computed Age Assurance state, if available, and any additional metadata needed to compute Age Assurance state client-side.

    ## Parameters

    - `countryCode` (string, required)
    - `regionCode` (string, optional)

  """
  @spec get_state(Client.t(), map() | keyword()) :: {:ok, map()} | {:error, term()}
  def get_state(client, params \\ []) do
    with {:ok, validated} <- validate_get_state(params) do
      client
      |> Client.query(
        "app.bsky.ageassurance.getState",
        Runtime.encode_query_params(validated, @fields_get_state)
      )
    end
  end

  defp validate_get_state(params) do
    {attrs, _extra} = Runtime.atomize(params, @fields_get_state)

    {f_country_code, e1} = Runtime.get_string(attrs, :country_code, "countryCode", required: true)
    {f_region_code, e2} = Runtime.get_string(attrs, :region_code, "regionCode", [])

    errors = e1 ++ e2

    if errors == [] do
      {:ok, %{country_code: f_country_code, region_code: f_region_code}}
    else
      {:error, errors}
    end
  end
end
