# Generated by `mix exosphere.gen.lexicons` — DO NOT EDIT.
# Regenerate with: mix exosphere.gen.lexicons

defmodule Exosphere.Bsky.Labeler.Service do
  @moduledoc """
  Record schema `app.bsky.labeler.service`.


  ## Fields

  - `created_at`: string, format `datetime`
  - `labels`: union
  - `policies`: ref `app.bsky.labeler.defs#labelerPolicies`
  - `reason_types`: The set of report reason 'codes' which are in-scope for this service to review and action. These usually align to policy categories. If not defined (distinct from empty array), all reason types are allowed.
  - `subject_collections`: Set of record types (collection NSIDs) which can be reported to this service. If not defined (distinct from empty array), default is any record type.
  - `subject_types`: The set of subject types (account, record, etc) this service accepts reports on.
  """

  alias Exosphere.Bsky.Runtime
  @type_id "app.bsky.labeler.service"
  @variants_labels [
    {"com.atproto.label.defs#selfLabels", Exosphere.ATProto.Label.Defs.SelfLabels}
  ]

  @enforce_keys [:created_at, :policies]
  defstruct created_at: nil,
            labels: nil,
            policies: nil,
            reason_types: nil,
            subject_collections: nil,
            subject_types: nil,
            extra: %{}

  @type t :: %__MODULE__{
          created_at: String.t(),
          labels: Exosphere.ATProto.Label.Defs.SelfLabels.t() | map() | nil,
          policies: Exosphere.Bsky.Labeler.Defs.LabelerPolicies.t(),
          reason_types: [term()] | nil,
          subject_collections: [String.t()] | nil,
          subject_types: [term()] | nil
        }

  @fields %{
    created_at: "createdAt",
    labels: "labels",
    policies: "policies",
    reason_types: "reasonTypes",
    subject_collections: "subjectCollections",
    subject_types: "subjectTypes"
  }

  @doc "The lexicon type ID (nsid or nsid#def) this module was generated from."
  @spec type_id() :: String.t()
  def type_id, do: @type_id

  @doc """
  Build and validate from a map of attrs (atom or wire string keys).

  Unknown keys are kept in `extra` and re-emitted by `to_map/1`.
  """
  @spec new(map()) :: {:ok, t()} | {:error, [{path :: String.t(), message :: String.t()}]}
  def new(attrs) when is_map(attrs) do
    {attrs, extra} = Runtime.atomize(attrs, @fields)

    {f_created_at, e1} =
      Runtime.get_string(attrs, :created_at, "createdAt", required: true, format: "datetime")

    {f_labels, e2} = Runtime.get_union(attrs, :labels, "labels", [], @variants_labels)

    {f_policies, e3} =
      Runtime.get_ref(
        attrs,
        :policies,
        "policies",
        [required: true],
        Exosphere.Bsky.Labeler.Defs.LabelerPolicies
      )

    {f_reason_types, e4} =
      Runtime.get_array(attrs, :reason_types, "reasonTypes", [], fn v, _path -> {:ok, v} end)

    {f_subject_collections, e5} =
      Runtime.get_array(attrs, :subject_collections, "subjectCollections", [], fn v, path ->
        Runtime.item_string(v, path, format: "nsid")
      end)

    {f_subject_types, e6} =
      Runtime.get_array(attrs, :subject_types, "subjectTypes", [], fn v, _path -> {:ok, v} end)

    errors = e1 ++ e2 ++ e3 ++ e4 ++ e5 ++ e6

    if errors == [] do
      {:ok,
       %__MODULE__{
         created_at: f_created_at,
         labels: f_labels,
         policies: f_policies,
         reason_types: f_reason_types,
         subject_collections: f_subject_collections,
         subject_types: f_subject_types,
         extra: extra
       }}
    else
      {:error, errors}
    end
  end

  def new(_), do: {:error, [{"", "expected a map"}]}

  @doc "Build and validate, raising ArgumentError on error."
  @spec new!(map()) :: t()
  def new!(attrs) do
    case new(attrs) do
      {:ok, struct} -> struct
      {:error, errors} -> raise ArgumentError, "invalid #{@type_id}: #{inspect(errors)}"
    end
  end

  @doc "Encode to the wire-format map. Record schemas include \"$type\"."
  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = struct) do
    %{}
    |> Runtime.put_field("createdAt", struct.created_at)
    |> Runtime.put_field("labels", struct.labels, &encode_labels/1)
    |> Runtime.put_field("policies", struct.policies, &encode_policies/1)
    |> Runtime.put_field("reasonTypes", struct.reason_types)
    |> Runtime.put_field("subjectCollections", struct.subject_collections)
    |> Runtime.put_field("subjectTypes", struct.subject_types)
    |> Map.merge(struct.extra)
    |> Map.put("$type", @type_id)
  end

  @doc "Decode a wire-format map. Delegates to new/1."
  @spec from_map(map()) :: {:ok, t()} | {:error, [{path :: String.t(), message :: String.t()}]}
  def from_map(map) when is_map(map), do: new(map)
  def from_map(_), do: {:error, [{"", "expected a map"}]}

  defp encode_ref(v, module) when is_struct(v, module), do: module.to_map(v)

  defp encode_ref(v, _module) when is_map(v), do: v

  defp encode_labels(v) when is_struct(v, Exosphere.ATProto.Label.Defs.SelfLabels),
    do:
      Map.put(
        Exosphere.ATProto.Label.Defs.SelfLabels.to_map(v),
        "$type",
        "com.atproto.label.defs#selfLabels"
      )

  defp encode_labels(v) when is_map(v), do: v

  defp encode_policies(v), do: encode_ref(v, Exosphere.Bsky.Labeler.Defs.LabelerPolicies)
end