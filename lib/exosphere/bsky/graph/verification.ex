# Generated by `mix exosphere.gen.lexicons` — DO NOT EDIT.
# Regenerate with: mix exosphere.gen.lexicons

defmodule Exosphere.Bsky.Graph.Verification do
  @moduledoc """
  Record schema `app.bsky.graph.verification`.


  ## Fields

  - `created_at`: Date of when the verification was created.
  - `display_name`: Display name of the subject the verification applies to at the moment of verifying, which might not be the same at the time of viewing. The verification is only valid if the current displayName matches the one at the time of verifying.
  - `handle`: Handle of the subject the verification applies to at the moment of verifying, which might not be the same at the time of viewing. The verification is only valid if the current handle matches the one at the time of verifying.
  - `subject`: DID of the subject the verification applies to.
  """

  alias Exosphere.Bsky.Runtime
  @type_id "app.bsky.graph.verification"

  @enforce_keys [:created_at, :display_name, :handle, :subject]
  defstruct created_at: nil, display_name: nil, handle: nil, subject: nil, extra: %{}

  @type t :: %__MODULE__{
          created_at: String.t(),
          display_name: String.t(),
          handle: String.t(),
          subject: String.t()
        }

  @fields %{
    created_at: "createdAt",
    display_name: "displayName",
    handle: "handle",
    subject: "subject"
  }

  @doc "The lexicon type ID (nsid or nsid#def) this module was generated from."
  @spec type_id() :: String.t()
  def type_id, do: @type_id

  @doc """
  Build and validate from a map of attrs (atom or wire string keys).

  Unknown keys are kept in `extra` and re-emitted by `to_map/1`.
  """
  @spec new(map()) :: {:ok, t()} | {:error, [{path :: String.t(), message :: String.t()}]}
  def new(attrs) when is_map(attrs) do
    {attrs, extra} = Runtime.atomize(attrs, @fields)

    {f_created_at, e1} =
      Runtime.get_string(attrs, :created_at, "createdAt", required: true, format: "datetime")

    {f_display_name, e2} = Runtime.get_string(attrs, :display_name, "displayName", required: true)

    {f_handle, e3} =
      Runtime.get_string(attrs, :handle, "handle", required: true, format: "handle")

    {f_subject, e4} =
      Runtime.get_string(attrs, :subject, "subject", required: true, format: "did")

    errors = e1 ++ e2 ++ e3 ++ e4

    if errors == [] do
      {:ok,
       %__MODULE__{
         created_at: f_created_at,
         display_name: f_display_name,
         handle: f_handle,
         subject: f_subject,
         extra: extra
       }}
    else
      {:error, errors}
    end
  end

  def new(_), do: {:error, [{"", "expected a map"}]}

  @doc "Build and validate, raising ArgumentError on error."
  @spec new!(map()) :: t()
  def new!(attrs) do
    case new(attrs) do
      {:ok, struct} -> struct
      {:error, errors} -> raise ArgumentError, "invalid #{@type_id}: #{inspect(errors)}"
    end
  end

  @doc "Encode to the wire-format map. Record schemas include \"$type\"."
  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = struct) do
    %{}
    |> Runtime.put_field("createdAt", struct.created_at)
    |> Runtime.put_field("displayName", struct.display_name)
    |> Runtime.put_field("handle", struct.handle)
    |> Runtime.put_field("subject", struct.subject)
    |> Map.merge(struct.extra)
    |> Map.put("$type", @type_id)
  end

  @doc "Decode a wire-format map. Delegates to new/1."
  @spec from_map(map()) :: {:ok, t()} | {:error, [{path :: String.t(), message :: String.t()}]}
  def from_map(map) when is_map(map), do: new(map)
  def from_map(_), do: {:error, [{"", "expected a map"}]}
end
