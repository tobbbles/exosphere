# Generated by `mix exosphere.gen.lexicons` — DO NOT EDIT.
# Regenerate with: mix exosphere.gen.lexicons

defmodule Exosphere.Bsky.Embed.RecordWithMedia do
  @moduledoc """
  A representation of a record embedded in a Bluesky record (eg, a post), alongside other compatible embeds. For example, a quote post and image, or a quote post and external URL card.

  Object schema `app.bsky.embed.recordWithMedia`.


  ## Fields

  - `media`: union
  - `record`: ref `app.bsky.embed.record`
  """

  alias Exosphere.Bsky.Runtime

  alias Exosphere.Bsky.Embed.External
  alias Exosphere.Bsky.Embed.Gallery
  alias Exosphere.Bsky.Embed.Images
  alias Exosphere.Bsky.Embed.Record
  alias Exosphere.Bsky.Embed.Video
  @type_id "app.bsky.embed.recordWithMedia"
  @variants_media [
    {"app.bsky.embed.images", Exosphere.Bsky.Embed.Images},
    {"app.bsky.embed.video", Exosphere.Bsky.Embed.Video},
    {"app.bsky.embed.gallery", Exosphere.Bsky.Embed.Gallery},
    {"app.bsky.embed.external", Exosphere.Bsky.Embed.External}
  ]

  @enforce_keys [:media, :record]
  defstruct media: nil, record: nil, extra: %{}

  @type t :: %__MODULE__{
          media: Images.t() | Video.t() | Gallery.t() | External.t() | map(),
          record: Record.t()
        }

  @fields %{media: "media", record: "record"}

  @doc "The lexicon type ID (nsid or nsid#def) this module was generated from."
  @spec type_id() :: String.t()
  def type_id, do: @type_id

  @doc """
  Build and validate from a map of attrs (atom or wire string keys).

  Unknown keys are kept in `extra` and re-emitted by `to_map/1`.
  """
  @spec new(map()) :: {:ok, t()} | {:error, [{path :: String.t(), message :: String.t()}]}
  def new(attrs) when is_map(attrs) do
    {attrs, extra} = Runtime.atomize(attrs, @fields)

    {f_media, e1} = Runtime.get_union(attrs, :media, "media", [required: true], @variants_media)

    {f_record, e2} =
      Runtime.get_ref(attrs, :record, "record", [required: true], Exosphere.Bsky.Embed.Record)

    errors = e1 ++ e2

    if errors == [] do
      {:ok, %__MODULE__{media: f_media, record: f_record, extra: extra}}
    else
      {:error, errors}
    end
  end

  def new(_), do: {:error, [{"", "expected a map"}]}

  @doc "Build and validate, raising ArgumentError on error."
  @spec new!(map()) :: t()
  def new!(attrs) do
    case new(attrs) do
      {:ok, struct} -> struct
      {:error, errors} -> raise ArgumentError, "invalid #{@type_id}: #{inspect(errors)}"
    end
  end

  @doc "Encode to the wire-format map."
  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = struct) do
    %{}
    |> Runtime.put_field("media", struct.media, &encode_media/1)
    |> Runtime.put_field("record", struct.record, &encode_record/1)
    |> Map.merge(struct.extra)
  end

  @doc "Decode a wire-format map. Delegates to new/1."
  @spec from_map(map()) :: {:ok, t()} | {:error, [{path :: String.t(), message :: String.t()}]}
  def from_map(map) when is_map(map), do: new(map)
  def from_map(_), do: {:error, [{"", "expected a map"}]}

  defp encode_ref(v, module) when is_struct(v, module), do: module.to_map(v)

  defp encode_ref(v, _module) when is_map(v), do: v

  defp encode_media(v) when is_struct(v, Exosphere.Bsky.Embed.Images),
    do: Map.put(Images.to_map(v), "$type", "app.bsky.embed.images")

  defp encode_media(v) when is_struct(v, Exosphere.Bsky.Embed.Video),
    do: Map.put(Video.to_map(v), "$type", "app.bsky.embed.video")

  defp encode_media(v) when is_struct(v, Exosphere.Bsky.Embed.Gallery),
    do: Map.put(Gallery.to_map(v), "$type", "app.bsky.embed.gallery")

  defp encode_media(v) when is_struct(v, Exosphere.Bsky.Embed.External),
    do: Map.put(External.to_map(v), "$type", "app.bsky.embed.external")

  defp encode_media(v) when is_map(v), do: v

  defp encode_record(v), do: encode_ref(v, Exosphere.Bsky.Embed.Record)
end
