# Generated by `mix exosphere.gen.lexicons` — DO NOT EDIT.
# Regenerate with: mix exosphere.gen.lexicons

defmodule Exosphere.Bsky.Embed.External.External do
  @moduledoc """
  Object schema `app.bsky.embed.external##external`.


  ## Fields

  - `associated_refs`: StrongRefs (uri+cid) of the Atmosphere records that backed this view.
  - `description`: string
  - `thumb`: blob
  - `title`: string
  - `uri`: string, format `uri`
  """

  alias Exosphere.Bsky.Runtime
  @type_id "app.bsky.embed.external#external"

  @enforce_keys [:description, :title, :uri]
  defstruct associated_refs: nil, description: nil, thumb: nil, title: nil, uri: nil, extra: %{}

  @type t :: %__MODULE__{
          associated_refs: [Exosphere.ATProto.Repo.StrongRef.t()] | nil,
          description: String.t(),
          thumb: term() | nil,
          title: String.t(),
          uri: String.t()
        }

  @fields %{
    associated_refs: "associatedRefs",
    description: "description",
    thumb: "thumb",
    title: "title",
    uri: "uri"
  }

  @doc "The lexicon type ID (nsid or nsid#def) this module was generated from."
  @spec type_id() :: String.t()
  def type_id, do: @type_id

  @doc """
  Build and validate from a map of attrs (atom or wire string keys).

  Unknown keys are kept in `extra` and re-emitted by `to_map/1`.
  """
  @spec new(map()) :: {:ok, t()} | {:error, [{path :: String.t(), message :: String.t()}]}
  def new(attrs) when is_map(attrs) do
    {attrs, extra} = Runtime.atomize(attrs, @fields)

    {f_associated_refs, e1} =
      Runtime.get_array(attrs, :associated_refs, "associatedRefs", [], fn v, path ->
        Runtime.item_ref(v, path, Exosphere.ATProto.Repo.StrongRef)
      end)

    {f_description, e2} = Runtime.get_string(attrs, :description, "description", required: true)
    {f_thumb, e3} = Runtime.get_any(attrs, :thumb, "thumb", [])
    {f_title, e4} = Runtime.get_string(attrs, :title, "title", required: true)
    {f_uri, e5} = Runtime.get_string(attrs, :uri, "uri", required: true, format: "uri")

    errors = e1 ++ e2 ++ e3 ++ e4 ++ e5

    if errors == [] do
      {:ok,
       %__MODULE__{
         associated_refs: f_associated_refs,
         description: f_description,
         thumb: f_thumb,
         title: f_title,
         uri: f_uri,
         extra: extra
       }}
    else
      {:error, errors}
    end
  end

  def new(_), do: {:error, [{"", "expected a map"}]}

  @doc "Build and validate, raising ArgumentError on error."
  @spec new!(map()) :: t()
  def new!(attrs) do
    case new(attrs) do
      {:ok, struct} -> struct
      {:error, errors} -> raise ArgumentError, "invalid #{@type_id}: #{inspect(errors)}"
    end
  end

  @doc "Encode to the wire-format map."
  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = struct) do
    %{}
    |> Runtime.put_field("associatedRefs", struct.associated_refs, &encode_associated_refs/1)
    |> Runtime.put_field("description", struct.description)
    |> Runtime.put_field("thumb", struct.thumb)
    |> Runtime.put_field("title", struct.title)
    |> Runtime.put_field("uri", struct.uri)
    |> Map.merge(struct.extra)
  end

  @doc "Decode a wire-format map. Delegates to new/1."
  @spec from_map(map()) :: {:ok, t()} | {:error, [{path :: String.t(), message :: String.t()}]}
  def from_map(map) when is_map(map), do: new(map)
  def from_map(_), do: {:error, [{"", "expected a map"}]}

  defp encode_ref(v, module) when is_struct(v, module), do: module.to_map(v)

  defp encode_ref(v, _module) when is_map(v), do: v

  defp encode_associated_refs(values), do: Enum.map(values, &encode_associated_refs_item/1)

  defp encode_associated_refs_item(v), do: encode_ref(v, Exosphere.ATProto.Repo.StrongRef)
end